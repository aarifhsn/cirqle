// listing page- posts can be uploaded

// if logged in using google, then if input password next time, then it should ask to login with google instead of showing invalid password

// from auth profile, if i edit privacy of the posts, state dont immidiate update, need to refresh

// single jobs page create
